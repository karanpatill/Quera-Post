<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quera Posts</title>
    <link rel="stylesheet" href="/index.css">
</head>
<body>
    <h1 class="LOGO" >Quera Posts</h1>
    <% for (let p of post) { %>
        <div class="post">
            <h2 class = "user"><%= p.username %></h2>
            <img src="<%= p.img %>" alt="image" class="img">
            <br>

            <p class = "cont"><%= p.content %></p>
            <br>
            
            <button ><a href="http://localhost:8000/posts/<%=p.id%>" class="expandfd">read more</a></button>
            <br>
            <button ><a href="http://localhost:8000/posts/<%=p.id%>/edit" class="expandfd">edit</a></button>
            <br>
            <form method="POST" action="http://localhost:8000/posts/<%=p.id%>?_method=DELETE">
                <button>Delete Post</button>
            </form>
        </div>
    <% } %>

    <button ><a href="http://localhost:8000/posts/new" class="new">New Post</a></button>
</body>
</html>
